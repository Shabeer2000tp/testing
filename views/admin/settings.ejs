<div class="d-flex justify-content-between align-items-center mb-4">
    <div>
        <h1 class="welcome-header h3 mb-0">Application Settings</h1>
        <p class="text-muted">Configure global settings for Sprint Sync.</p>
    </div>
</div>

<div class="row justify-content-center">
    <div class="col-lg-8">
        <div class="card form-card">
            <div class="card-header">
                <i class="fas fa-cogs me-2"></i>Sprint Creation Mode
            </div>
            <div class="card-body">
                <form action="/admin/settings" method="POST">
                    <input type="hidden" name="key" value="sprintCreation">
                    <div class="mb-3">
                        <label for="sprintMode" class="form-label">Select how sprints should be managed:</label>
                        <select class="form-select" id="sprintMode" name="value">
                            <option value="global" <%= (sprintSetting && sprintSetting.value === 'global') ? 'selected' : '' %>>
                                Global (Admin creates all sprints)
                            </option>
                            <option value="team" <%= (sprintSetting && sprintSetting.value === 'team') ? 'selected' : '' %>>
                                Team-Specific (Students create their own sprints)
                            </option>
                        </select>
                    </div>
                    <div class="d-grid">
                        <button type="submit" class="btn btn-primary">Save Settings</button>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>