<link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600&display=swap" rel="stylesheet">

<style>

/* -------------------------------------------------
   COLOR PALETTE (Black + Silver + Subtle Blue Accent)
--------------------------------------------------- */
:root {
  --black: #000000;
  --dark-gray: #1A1A1A;
  --gray: #3A3A3A;
  --light-gray: #D9D9D9;
  --silver: #E6E6E6;
  --accent: #6C63FF;  /* Main Purple Theme */
  --accent-light: #A29BFE; /* Light Purple for buttons/hovers */
  --white: #FFFFFF;
  --transition-speed: 0.2s; /* Standardized transition duration */
}

/* -------------------------------------------------
   GLOBAL PAGE RESET
--------------------------------------------------- */
body {
  background-color: var(--dark-gray);
  color: var(--white);
  margin: 0;
  font-family: "Inter", sans-serif;
  font-size: 16px; /* Set a consistent base font size */
  line-height: 1.6; /* Improve overall text readability */
  letter-spacing: .3px;
}

.app-body {
  display: flex;
}

.main-content {
  flex-grow: 1;
  padding: 28px;
  margin-left: 250px; /* Match sidebar width */
  width: calc(100% - 250px);
}

a {
  text-decoration: none;
  color: var(--accent);
}

a:hover {
  color: var(--accent-light);
  transition: color var(--transition-speed) ease;
}

/* --- Accessible Focus States --- */
a:focus-visible, .btn:focus-visible, .form-select:focus-visible, .nav-link:focus-visible {
  outline: 2px solid var(--accent) !important;
  outline-offset: 2px;
}

.text-muted {
  color: var(--white) !important; /* Maximize contrast for readability */
}

/* Make form helper text use the accent color for better visibility and distinction */
.form-text {
  color: var(--accent) !important;
}

.text-danger {
  color: #e74a3b !important; /* Use alert red for visibility */
}

.text-success {
  color: #1cc88a !important; /* Use alert green for visibility */
}

.text-small {
  font-size: 0.875em; /* 14px if base is 16px */
  color: var(--silver) !important;
  opacity: 0.8;
}


/* -------------------------------------------------
   NAVBAR (Black Academic Style)
   ** This is now the Sidebar **
--------------------------------------------------- */
.sidebar {
  width: 250px;
  height: 100vh;
  position: fixed;
  top: 0;
  left: 0;
  background-color: var(--black);
  z-index: 1000;
}

/* Sidebar Logo */
.sidebar-logo {
  height: 70px; /* Adjust as needed */
  width: auto;
}

.sidebar-logo-container {
  padding: 1rem 1.5rem;
  text-align: left;
}

/* Sidebar Navigation Links */
.sidebar .nav-link {
  color: var(--silver); /* Default link color */
  font-size: 15px;
  padding: 10px 16px;
  transition: background-color var(--transition-speed) ease, color var(--transition-speed) ease;
}

.sidebar .nav-link:hover {
  background-color: var(--gray); /* Darker gray on hover */
  color: var(--white); /* Brighter text on hover */
  border-radius: 6px;
}

.sidebar .nav-link.active {
  background-color: var(--accent); /* Accent color for active link */
  color: var(--black); /* Dark text on accent background */
  font-weight: 600;
  border-radius: 6px;
}

/* Sidebar Logout Button */
.sidebar .btn-outline-light {
    color: var(--silver);
    border-color: var(--gray);
}

.sidebar .btn-outline-light:hover {
    color: var(--white);
    background-color: var(--gray);
    border-color: var(--gray);
}

/* -------------------------------------------------
   PAGE CONTAINERS
--------------------------------------------------- */
.container-custom {
  max-width: 1250px;
  margin: auto;
  padding: 28px;
}

/* -------------------------------------------------
   UNIFIED CARD DESIGN
--------------------------------------------------- */
.card {
  background-color: var(--dark-gray);
  color: var(--white);
  border-radius: 10px;
  border: 1px solid var(--gray);
  margin-bottom: 18px;
  overflow: hidden; /* Ensures content respects border-radius */
  transition: transform var(--transition-speed) ease, box-shadow var(--transition-speed) ease;
}

.card-body {
  padding: 24px;
}

.card-header {
  padding: 16px 24px;
  background-color: rgba(0,0,0,0.2);
  border-bottom: 1px solid var(--gray);
}

/* --- Filter Card Style --- */
.filter-card {
  background-color: rgba(0,0,0,0.2);
  padding: 16px;
  border-radius: 8px;
  margin-bottom: 24px;
}
.filter-card .form-group {
  margin-bottom: 0;
}
.filter-card .form-label {
  font-size: 14px;
  margin-bottom: 4px;
}

/* --- STAT CARD STYLES (Team Detail Page) --- */
.stat-card {
  background-color: var(--dark-gray);
  border: 1px solid var(--gray);
  border-left-width: 4px;
  border-radius: 8px;
  transition: all var(--transition-speed) ease;
  position: relative;
  overflow: hidden;
}
.stat-card:hover {
  transform: translateY(-3px);
  border-color: var(--accent);
  box-shadow: 0 4px 15px rgba(0,0,0,0.2);
}
.stat-card .card-body {
  padding: 20px;
}
.stat-card-title {
  font-size: 14px;
  font-weight: 500;
  color: var(--silver);
  text-transform: uppercase;
  letter-spacing: 0.5px;
}
.stat-card-value {
  font-size: 24px;
  font-weight: 600;
  color: var(--white);
}
.stat-card-icon {
  position: absolute;
  right: 20px;
  top: 50%;
  transform: translateY(-50%);
  font-size: 32px;
  color: rgba(255, 255, 255, 0.05);
}

/* Badge Style for "All" */
.badge {
  background-color: #444;
  color: #fff;
  padding: 5px 10px;
  border-radius: 6px;
  font-size: 12px;
}


/* --- NEW TEAM CARD DESIGN --- */

.team-card-container {
  display: flex;
  flex-wrap: wrap;
  gap: 24px;
  margin-top: 1rem;
}

.team-card {
  background-color: var(--dark-gray);
  border: 1px solid var(--gray);
  border-radius: 8px;
  display: flex;
  flex-direction: column;
  transition: all var(--transition-speed) ease-in-out;
  flex: 1 1 calc(33.333% - 16px); /* Default: 3 cards per row. 16px = 2/3 of 24px gap */
  overflow: hidden; /* Ensures child elements respect the border radius */
}

.team-card:hover {
  transform: translateY(-5px);
  box-shadow: 0 6px 20px rgba(0,0,0,0.4);
  border-color: var(--accent);
}

.team-card-header {
  display: flex;
  justify-content: space-between;
  align-items: flex-start;
  padding: 20px 20px 16px 20px;
  border-bottom: 1px solid var(--gray);
}

.team-card-header h5 {
  margin: 0;
  font-weight: 600;
  font-size: 1.1rem;
  line-height: 1.3;
}

.team-card-body {
  padding: 16px 20px;
  display: flex;
  flex-direction: column;
  gap: 16px;
  flex-grow: 1; /* Allows body to fill space */
}

.team-members {
  font-size: 14px;
  color: var(--light-gray);
  line-height: 1.6;
}

.team-members strong {
  color: var(--silver);
  font-weight: 500;
}

.team-card-actions {
  margin-top: auto; /* Pushes actions to the bottom */
  padding: 16px 20px;
  border-top: 1px solid var(--gray);
  background-color: rgba(0,0,0,0.2);
  display: flex;
  gap: 10px;
}

/* "Create New Team" Card Style */
.create-team-card {
  flex: 1 1 calc(33.333% - 16px);
  border: 2px dashed var(--light-gray);
  background-color: transparent;
  justify-content: center;
  align-items: center;
  text-align: center;
  color: var(--light-gray);
  min-height: 350px; /* Give it a consistent height */
  transition: all var(--transition-speed) ease-in-out;
}

.create-team-card:hover {
  transform: translateY(-5px);
  border-color: var(--accent);
  background-color: var(--gray);
  color: var(--white);
  cursor: pointer;
}

.create-team-card i {
  font-size: 40px;
  margin-bottom: 16px;
}

/* Refined Progress Bar Styles */

.progress-header {
  display: flex;
  justify-content: space-between;
  font-size: 13px;
  margin-bottom: 6px;
  color: var(--silver);
}

.progress-title {
  font-weight: 500;
}

.progress {
  height: 8px;
  background-color: var(--dark-gray);
  border-radius: 4px;
  overflow: hidden;
}

.progress-bar {
  width: calc(var(--p, 0) * 1%); /* Use CSS variable --p as percent fallback */
  transition: width 1.2s cubic-bezier(0.25, 0.46, 0.45, 0.94); /* Smooth animation */
  height: 100%;
  display: flex;
  background-color: var(--accent); /* Default color */
}

/* Progress Bar Color Variants */
.progress-bar-danger {
  background-color: #e74a3b; /* Red */
}
.progress-bar-warning {
  background-color: #f6c23e; /* Yellow */
}
.progress-bar-success {
  background-color: #28a745; /* Green */
}

/* Member chip styles used on team cards */
.member-list {
  margin-top: 8px;
  display: flex;
  gap: 8px;
  flex-wrap: wrap;
}
.member-chip {
  display: inline-block;
  padding: 4px 8px;
  background: rgba(255,255,255,0.03);
  border: 1px solid rgba(255,255,255,0.03);
  border-radius: 12px;
  font-size: 12px;
  color: var(--accent);
}

/* --- Responsive Card Overrides --- */
/* Tablet - 2 cards per row */
@media (max-width: 992px) {
  .team-card, .create-team-card {
    flex-basis: calc(50% - 12px); /* 12px = 1/2 of 24px gap */
  }
}

/* Mobile - 1 card per row */
@media (max-width: 768px) {
  .team-card, .create-team-card {
    flex-basis: 100%;
  }
}

/* -------------------------------------------------
   TABLE DESIGN (Elevated Style)
--------------------------------------------------- */
.table {
  margin-bottom: 0; /* Important for cards with p-0 */
}

.table thead th {
  background-color: rgba(0,0,0,0.2);
  color: var(--silver);
  font-weight: 500;
  font-size: 14px;
  text-transform: uppercase;
  letter-spacing: 0.5px;
  border-bottom-width: 1px;
  border-top: none;
}

.table tbody tr {
  background-color: transparent;
  border-bottom: 1px solid var(--gray);
  color: #ffffff;
  transition: background-color var(--transition-speed) ease-in-out;
}

.table tbody tr:hover {
  background-color: var(--gray); /* subtle, refined hover */
}

/* Style for recipient names in tables to make them stand out */
.recipient-name {
  color: var(--accent-light);
  font-weight: 500;
}

/* -------------------------------------------------
   LIST GROUPS
--------------------------------------------------- */
.list-group-item {
  background-color: transparent; /* Blend into parent card */
  color: var(--silver);
  border-color: var(--dark-gray) !important; /* Subtle separator, override Bootstrap */
}

/* Hover effect for interactive list items */
a.list-group-item:hover, button.list-group-item:hover {
  background-color: var(--dark-gray);
  color: var(--white);
  cursor: pointer;
}

/* -------------------------------------------------
   BUTTONS
--------------------------------------------------- */
.btn-accent {
  background-color: var(--accent-light);
  border: none;
  color: var(--white);
  padding: 8px 14px;
  border-radius: 6px;
  font-weight: 600;
  transition: background-color var(--transition-speed) ease-in-out;
}

.btn-accent:hover {
  background-color: var(--accent);
}

.btn-action {
  padding: 5px 10px;
  font-size: 14px;
  border-radius: 5px;
  margin-right: 5px;
  color: var(--silver);
  border: 1px solid var(--gray);
}

.btn-action:hover {
  background-color: var(--gray);
  color: var(--white);
}

/* Override Bootstrap Primary Button for consistency */
.btn-primary {
  background-color: var(--accent-light);
  border-color: var(--accent-light);
  color: var(--white) !important;
  font-weight: 600;
}

.btn-primary:hover,
.btn-primary:focus,
.btn-primary:active {
  background-color: var(--accent) !important;
  border-color: var(--accent) !important;
  color: var(--white) !important;
  box-shadow: none !important;
}

/* --- PAGINATION STYLES --- */
.pagination {
  margin-bottom: 0;
}
.page-item .page-link {
  background-color: var(--gray);
  border: 1px solid var(--dark-gray);
  color: var(--silver);
  margin: 0 2px;
  border-radius: 6px;
  transition: all var(--transition-speed) ease;
}
.page-item.active .page-link {
  background-color: var(--accent);
  border-color: var(--accent);
  color: var(--white);
  font-weight: 600;
}
.page-item.disabled .page-link {
  background-color: var(--dark-gray);
  color: var(--gray);
}

/* -------------------------------------------------
   ALERTS
--------------------------------------------------- */
.alert {
  background-color: var(--gray);
  color: var(--white);
  border: 1px solid var(--dark-gray);
  border-left-width: 4px;
}

.alert-success {
  border-left-color: #1cc88a; /* Green */
}

.alert-danger {
  border-left-color: #e74a3b; /* Red */
}

.alert-info {
  border-left-color: var(--accent); /* Purple */
}

.alert-warning {
  border-left-color: #f6c23e; /* Yellow */
}

/* -------------------------------------------------
   INPUTS + FORMS
--------------------------------------------------- */
/* -------------------------------------------------
   FIX FORM INPUT + MULTI-SELECT VISIBILITY (FINAL)
--------------------------------------------------- */

/* Text fields & selects base style */
input, select, textarea, .form-control, .form-select {
  background-color: var(--gray) !important;
  border: 1px solid var(--light-gray) !important;
  color: var(--white) !important;
}

/* Placeholder color */
input::placeholder,
textarea::placeholder {
  color: var(--light-gray) !important;
  opacity: 1 !important;
}

/* Fix dropdown & multi-select list background */
select option,
.form-select option,
select[multiple] option {
  background-color: var(--gray) !important;
  color: var(--white) !important;
}

/* Highlight selected student inside multi-select */
select[multiple] option:checked {
  background-color: var(--accent) !important;
  color: var(--black) !important;
}

/* Hover color while navigating the multi-select */
select[multiple] option:hover {
  background-color: var(--accent-light) !important;
  color: var(--black) !important;
}

/* Remove the ugly blue outline (Windows default) */
input:focus, select:focus, textarea:focus {
  border-color: var(--accent) !important;
  box-shadow: 0 0 0 3px rgba(108, 99, 255, 0.35) !important;
  outline: none !important;
}


/* -------------------------------------------------
   SKELETON SHIMMER LOADING
--------------------------------------------------- */
.skeleton {
  background: linear-gradient(90deg, #D9D9D9 25%, #E6E6E6 50%, #D9D9D9 75%);
  background-size: 200% 100%;
  border-radius: 6px;
  animation: shimmer 1.4s infinite ease-in-out;
}

@keyframes shimmer {
  0% { background-position: 200% 0; }
  100% { background-position: -200% 0; }
}

/* Predefined skeleton shapes */
.skeleton-text { height: 14px; width: 100%; margin-bottom: 8px; }
.skeleton-title { height: 22px; width: 60%; margin-bottom: 12px; }
.skeleton-avatar { width: 40px; height: 40px; border-radius: 50%; }
.skeleton-card { height: 160px; width: 100%; border-radius: 10px; }

/* Loading visibility control */
.loading .hide-when-loading { display: none; }
.loading .show-when-loading { display: block; }

/* Smooth fade reveal for content */
.hide-when-loading {
  opacity: 0;
  transition: opacity 0.4s ease;
}
:not(.loading) .hide-when-loading {
  opacity: 1;
}

</style>

<style>
/* -------------------------------------------------
   CUSTOM SCROLLBAR
--------------------------------------------------- */
/* For Firefox */
html {
  scrollbar-width: thin;
  scrollbar-color: var(--gray) var(--dark-gray);
}

/* For Webkit browsers (Chrome, Safari, Edge) */
::-webkit-scrollbar {
  width: 12px;
}
::-webkit-scrollbar-track {
  background: var(--dark-gray);
}
::-webkit-scrollbar-thumb {
  background-color: var(--gray);
  border-radius: 20px;
  border: 3px solid var(--dark-gray);
}
::-webkit-scrollbar-thumb:hover {
  background-color: var(--accent);
}
</style>

<style>
/* -------------------------------------------------
   PAGE TRANSITION LOADER
--------------------------------------------------- */
.page-loader {
  position: fixed;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  background-color: var(--black);
  display: flex;
  justify-content: center;
  align-items: center;
  z-index: 9999;
  opacity: 1;
  transition: opacity 0.3s ease;
}

.spinner {
  /* Bouncing dots style */
  width: 12px;
  height: 12px;
  background-color: var(--accent);
  border-radius: 50%;
  position: relative;
  animation: bounce 1.4s infinite ease-in-out both;
  animation-delay: -0.16s;
}

.spinner::before,
.spinner::after {
  content: '';
  position: absolute;
  top: 0;
  width: 12px;
  height: 12px;
  background-color: var(--accent);
  border-radius: 50%;
  animation: bounce 1.4s infinite ease-in-out both;
}

.spinner::before {
  left: -20px;
  animation-delay: -0.32s;
}

.spinner::after {
  left: 20px;
}

@keyframes bounce {
  0%, 80%, 100% { transform: scale(0); }
  40% { transform: scale(1.0); }
}
</style>

<style>
/* -------------------------------------------------
   PAGE CONTENT FADE-IN
--------------------------------------------------- */
.content-to-fade {
  opacity: 0;
  transition: opacity 0.4s ease-in-out;
}
.content-to-fade.visible {
  opacity: 1;
}
</style>

<style>
/* -------------------------------------------------
   STATUS BADGES (Used in Task Lists)
--------------------------------------------------- */
.status-badge {
  display: inline-block;
  padding: 4px 10px;
  font-size: 12px;
  font-weight: 600;
  border-radius: 12px;
  text-transform: uppercase;
  letter-spacing: 0.5px;
}

.status-completed {
  background-color: rgba(40, 167, 69, 0.2);
  color: #28a745;
}
.status-on-track {
  background-color: rgba(78, 115, 223, 0.2);
  color: #4e73df;
}
.status-at-risk {
  background-color: rgba(246, 194, 62, 0.2);
  color: #f6c23e;
}
.status-overdue {
  background-color: rgba(231, 74, 59, 0.2);
  color: #e74a3b;
}
.status-pending {
  background-color: rgba(230, 230, 230, 0.2);
  color: #e6e6e6;
}
</style>
